from django.db import models

class User(models.Model):
    email = models.CharField(max_length=50)
    password = models.CharField(max_length=8)
    reg_date_hour = models.

class Video
    video_url = 

class Comment
    user_id =
    x_coord =
    y_coord =

class 
    
class Tag
